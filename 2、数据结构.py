# 21-创建一个列表，包含1到10的数字，并打印
# 22-从列表中提取所有的偶数
# 23-从列表中删除所有的奇数
# 24-将两个列表合并为一个列表
# 25-找出列表中的最大值和最小值
# 26-对列表进行排序（不使用内置排序函数）
# 27-计算列表中所有数字的平均值
# 28-检查列表中是否包含某个元素
# 29-将列表中的元素去重
# 30-将两个列表中的元素一一对应相加（等长列表）
# 31-创建一个字典，包含5个学生的姓名和成绩，并打印
# 32-根据字典中的值（成绩）对学生进行排序
# 33-统计一个字符串中每个字符出现的次数，使用字典
# 34-将两个字典合并
# 35-反转字典（键变成值，值变成键）
# 36-使用列表推导式创建一个包含1到10的平方的列表
# 37-使用列表推导式从列表中提取所有的偶数
# 38-使用字典推导式创建一个字典，键为1到10，值为键的平方
# 39-创建一个集合，包含1到10的数字，并打印
# 40-计算两个集合的交集、并集、差集